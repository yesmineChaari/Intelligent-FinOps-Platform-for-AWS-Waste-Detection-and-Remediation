PHASE 2 VALIDATION REPORT (LLM-ORIENTED)
Date: 2026-04-12
Workspace: pfa
Scope: End-to-end runtime + guardrail logic matrix + writer persistence path + DB helper smoke tests

====================================================================
1) EXECUTIVE SUMMARY
====================================================================
Current status: PARTIALLY WORKING -> now operational after migration.

- Initial run failed before any Phase 2 decisioning completed.
- Root blocker was missing enum values in Postgres type relationship_type_enum.
- After applying migrations/add_relationship_types.sql, end-to-end run succeeded.
- Guardrail decision logic passed a 10-scenario behavior matrix.
- Waste table write path works and is idempotent via ON CONFLICT.

Net: Phase 2 core logic and persistence are functioning, with a few spec deviations/risks documented below.

====================================================================
2) WHAT WAS TESTED
====================================================================
A) End-to-end run (python main.py)
- Test #1 (before migration): FAILED
- Error:
  asyncpg.exceptions.InvalidTextRepresentationError:
  invalid input value for enum relationship_type_enum: "monitored_by"

- Action taken:
  Applied all SQL statements in migrations/add_relationship_types.sql to Neon.

- Test #2 (after migration): PASSED
  Observed flow:
  - Phase 1 completes.
  - Phase 2 starts and processes non-skipped candidates.
  - Writer inserts/updates waste rows successfully.

B) Live waste-table verification
- Queried recent waste rows after successful run.
- Verified row for i-002 contains:
  action=DOWNSIZE, safety_status=SAFE,
  block_reason=NULL, pipeline_warning=false,
  redundancy_node=false, depth_of_block=NULL,
  detection_window='14' (string/text as required).

C) Guardrail logic matrix (controlled async harness, mocked query funcs)
- 10/10 scenarios PASSED:
  1. protected_backup -> NEEDS_REVIEW/protected_role
  2. protected_dependant_secondary -> NEEDS_REVIEW/protected_role
  3. capped_dependant_primary TERMINATE->DOWNSIZE (SAFE)
  4. TYPE_E redundancy target -> NEEDS_REVIEW/redundancy_node
  5. TYPE_A hard block + fallback available -> NEEDS_REVIEW + fallback DOWNSIZE
  6. TYPE_A hard block w/o fallback -> NEEDS_REVIEW only
  7. TYPE_B last LB target + TERMINATE -> NEEDS_REVIEW/last_routing_target
  8. TYPE_B with other targets -> SAFE passthrough
  9. TYPE_C caller -> warning only (pipeline_warning=true)
 10. no callers -> SAFE passthrough

D) Writer + query bulk insert contract (fake conn harness)
- Transaction context entered/exited.
- executemany called once for non-empty input.
- Tuple shape correct (17 params).
- detection_window converted to string before insert.
- SQL includes hardcoded NULL for waste_per_hour.
- SQL includes ON CONFLICT (instance_id, waste_type).
- Empty results correctly skip DB write.

E) Live DB helper smoke tests
- check_type_e_redundancy(): returns bool.
- get_upstream_callers(): returns list of dicts.
- count_active_lb_targets(): returns int.

====================================================================
3) CURRENTLY WORKING
====================================================================
- Phase 2 package imports and runs.
- Migration file exists and is executable.
- Recursive upstream CTE runs after enum migration.
- Type A/B/C/E classification behavior matches expected outcomes in test matrix.
- Protected/capped role handling works for DB spellings:
  dependant_primary, dependant_secondary.
- Terraform block generation rules work:
  - SAFE TERMINATE/STOP/DOWNSIZE produce terraform_block (DOWNSIZE requires target).
  - NEEDS_REVIEW without fallback has no terraform block.
  - NEEDS_REVIEW with fallback can produce fallback_terraform_block.
- Writer path is idempotent and uses single transaction + executemany.
- Required waste column mapping is correct:
  current_cost_per_hour/recommended_cost_per_hour used (not monthly columns).

====================================================================
4) NOT WORKING / GAPS / DEVIATIONS
====================================================================
1) Hard dependency on DB migration (observed failure until applied)
- Symptom: runtime error on enum literal monitored_by.
- Status: fixed in environment by applying migration.
- Risk: any environment missing this migration will fail immediately in Phase 2 queries.

2) Spec deviation in run_phase2 filtering
- Current code skips both SKIP and CLEAN from Phase 1.
- Prompt spec says to exclude SKIP only.
- Practical behavior: skipping CLEAN avoids writing no-waste rows; likely desirable,
  but this is still a documented deviation from the requested text.

3) Phase1->Phase2 metric naming bridge is heuristic
- Phase2 model fields are avg_cpu/peak_cpu/avg_ram,
  but Phase1 emits p95_cpu/p99_cpu/max_cpu/p95_ram.
- Current implementation maps:
  avg_cpu <- p95_cpu
  peak_cpu <- p99_cpu else max_cpu
  avg_ram <- p95_ram
- This is functionally working but semantically approximate.

4) detection_window is NOT NULL in waste table
- Writer/queries correctly stringify when present.
- If a future non-SKIP/CLEAN Phase1 result arrives with detection_window missing,
  insert could fail due to DB NOT NULL constraint.
- Not observed in current run, but possible edge risk.

====================================================================
5) POTENTIAL ROOT CAUSES FOR FUTURE FAILURES
====================================================================
- Schema drift between environments (enum/type values not migrated everywhere).
- Changes to resource_relationships semantics or role spellings not reflected in code.
- Additional relationship enums introduced in DB but not classified in TYPE_A/B/C/D/E sets.
- Data quality issues (missing recommended_type or missing detection_window where required).
- Runtime data distributions causing heavy recursive CTE cost on large graphs.

====================================================================
6) RECOMMENDED NEXT CHECKS
====================================================================
- Add an explicit startup preflight query in Phase 2 to verify required enum values
  before processing (fail-fast with actionable message).
- Decide policy for CLEAN rows explicitly:
  either keep skip CLEAN (current) and update spec/docs, or process CLEAN as spec says.
- Add automated tests (pytest) for guardrail matrix and writer tuple mapping.
- Consider explicit coercion/validation for detection_window before DB write to avoid
  NOT NULL failures from unexpected upstream payloads.

====================================================================
7) FINAL VERDICT
====================================================================
Phase 2 is operational and produces correct guardrail decisions + waste writes AFTER enum migration.
The main unresolved items are environment migration dependency and a documented behavior deviation
(SKIP + CLEAN filtering vs SKIP-only wording in spec).
